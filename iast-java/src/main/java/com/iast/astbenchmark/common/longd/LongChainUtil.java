package com.iast.astbenchmark.common.longd;

public class LongChainUtil {
    public String longChain0(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain1(data, i);
    }

    private String longChain1(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain2(data, i);
    }

    private String longChain2(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain3(data, i);
    }

    private String longChain3(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain4(data, i);
    }

    private String longChain4(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain5(data, i);
    }

    private String longChain5(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain6(data, i);
    }

    private String longChain6(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain7(data, i);
    }

    private String longChain7(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain8(data, i);
    }

    private String longChain8(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain9(data, i);
    }

    private String longChain9(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain10(data, i);
    }

    private String longChain10(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain11(data, i);
    }

    private String longChain11(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain12(data, i);
    }

    private String longChain12(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain13(data, i);
    }

    private String longChain13(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain14(data, i);
    }

    private String longChain14(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain15(data, i);
    }

    private String longChain15(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain16(data, i);
    }

    private String longChain16(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain17(data, i);
    }

    private String longChain17(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain18(data, i);
    }

    private String longChain18(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain19(data, i);
    }

    private String longChain19(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain20(data, i);
    }

    private String longChain20(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain21(data, i);
    }

    private String longChain21(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain22(data, i);
    }

    private String longChain22(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain23(data, i);
    }

    private String longChain23(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain24(data, i);
    }

    private String longChain24(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain25(data, i);
    }

    private String longChain25(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain26(data, i);
    }

    private String longChain26(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain27(data, i);
    }

    private String longChain27(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain28(data, i);
    }

    private String longChain28(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain29(data, i);
    }

    private String longChain29(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain30(data, i);
    }

    private String longChain30(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain31(data, i);
    }

    private String longChain31(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain32(data, i);
    }

    private String longChain32(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain33(data, i);
    }

    private String longChain33(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain34(data, i);
    }

    private String longChain34(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain35(data, i);
    }

    private String longChain35(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain36(data, i);
    }

    private String longChain36(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain37(data, i);
    }

    private String longChain37(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain38(data, i);
    }

    private String longChain38(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain39(data, i);
    }

    private String longChain39(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain40(data, i);
    }

    private String longChain40(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain41(data, i);
    }

    private String longChain41(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain42(data, i);
    }

    private String longChain42(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain43(data, i);
    }

    private String longChain43(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain44(data, i);
    }

    private String longChain44(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain45(data, i);
    }

    private String longChain45(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain46(data, i);
    }

    private String longChain46(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain47(data, i);
    }

    private String longChain47(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain48(data, i);
    }

    private String longChain48(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain49(data, i);
    }

    private String longChain49(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain50(data, i);
    }

    private String longChain50(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain51(data, i);
    }

    private String longChain51(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain52(data, i);
    }

    private String longChain52(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain53(data, i);
    }

    private String longChain53(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain54(data, i);
    }

    private String longChain54(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain55(data, i);
    }

    private String longChain55(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain56(data, i);
    }

    private String longChain56(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain57(data, i);
    }

    private String longChain57(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain58(data, i);
    }

    private String longChain58(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain59(data, i);
    }

    private String longChain59(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain60(data, i);
    }

    private String longChain60(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain61(data, i);
    }

    private String longChain61(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain62(data, i);
    }

    private String longChain62(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain63(data, i);
    }

    private String longChain63(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain64(data, i);
    }

    private String longChain64(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain65(data, i);
    }

    private String longChain65(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain66(data, i);
    }

    private String longChain66(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain67(data, i);
    }

    private String longChain67(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain68(data, i);
    }

    private String longChain68(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain69(data, i);
    }

    private String longChain69(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain70(data, i);
    }

    private String longChain70(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain71(data, i);
    }

    private String longChain71(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain72(data, i);
    }

    private String longChain72(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain73(data, i);
    }

    private String longChain73(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain74(data, i);
    }

    private String longChain74(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain75(data, i);
    }

    private String longChain75(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain76(data, i);
    }

    private String longChain76(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain77(data, i);
    }

    private String longChain77(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain78(data, i);
    }

    private String longChain78(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain79(data, i);
    }

    private String longChain79(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain80(data, i);
    }

    private String longChain80(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain81(data, i);
    }

    private String longChain81(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain82(data, i);
    }

    private String longChain82(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain83(data, i);
    }

    private String longChain83(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain84(data, i);
    }

    private String longChain84(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain85(data, i);
    }

    private String longChain85(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain86(data, i);
    }

    private String longChain86(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain87(data, i);
    }

    private String longChain87(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain88(data, i);
    }

    private String longChain88(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain89(data, i);
    }

    private String longChain89(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain90(data, i);
    }

    private String longChain90(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain91(data, i);
    }

    private String longChain91(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain92(data, i);
    }

    private String longChain92(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain93(data, i);
    }

    private String longChain93(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain94(data, i);
    }

    private String longChain94(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain95(data, i);
    }

    private String longChain95(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain96(data, i);
    }

    private String longChain96(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain97(data, i);
    }

    private String longChain97(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain98(data, i);
    }

    private String longChain98(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain99(data, i);
    }

    private String longChain99(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain100(data, i);
    }

    private String longChain100(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain101(data, i);
    }

    private String longChain101(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain102(data, i);
    }

    private String longChain102(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain103(data, i);
    }

    private String longChain103(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain104(data, i);
    }

    private String longChain104(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain105(data, i);
    }

    private String longChain105(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain106(data, i);
    }

    private String longChain106(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain107(data, i);
    }

    private String longChain107(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain108(data, i);
    }

    private String longChain108(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain109(data, i);
    }

    private String longChain109(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain110(data, i);
    }

    private String longChain110(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain111(data, i);
    }

    private String longChain111(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain112(data, i);
    }

    private String longChain112(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain113(data, i);
    }

    private String longChain113(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain114(data, i);
    }

    private String longChain114(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain115(data, i);
    }

    private String longChain115(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain116(data, i);
    }

    private String longChain116(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain117(data, i);
    }

    private String longChain117(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain118(data, i);
    }

    private String longChain118(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain119(data, i);
    }

    private String longChain119(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain120(data, i);
    }

    private String longChain120(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain121(data, i);
    }

    private String longChain121(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain122(data, i);
    }

    private String longChain122(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain123(data, i);
    }

    private String longChain123(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain124(data, i);
    }

    private String longChain124(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain125(data, i);
    }

    private String longChain125(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain126(data, i);
    }

    private String longChain126(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain127(data, i);
    }

    private String longChain127(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain128(data, i);
    }

    private String longChain128(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain129(data, i);
    }

    private String longChain129(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain130(data, i);
    }

    private String longChain130(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain131(data, i);
    }

    private String longChain131(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain132(data, i);
    }

    private String longChain132(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain133(data, i);
    }

    private String longChain133(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain134(data, i);
    }

    private String longChain134(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain135(data, i);
    }

    private String longChain135(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain136(data, i);
    }

    private String longChain136(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain137(data, i);
    }

    private String longChain137(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain138(data, i);
    }

    private String longChain138(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain139(data, i);
    }

    private String longChain139(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain140(data, i);
    }

    private String longChain140(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain141(data, i);
    }

    private String longChain141(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain142(data, i);
    }

    private String longChain142(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain143(data, i);
    }

    private String longChain143(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain144(data, i);
    }

    private String longChain144(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain145(data, i);
    }

    private String longChain145(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain146(data, i);
    }

    private String longChain146(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain147(data, i);
    }

    private String longChain147(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain148(data, i);
    }

    private String longChain148(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain149(data, i);
    }

    private String longChain149(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain150(data, i);
    }

    public String longChain150(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain151(data, i);
    }

    private String longChain151(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain152(data, i);
    }

    private String longChain152(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain153(data, i);
    }

    private String longChain153(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain154(data, i);
    }

    private String longChain154(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain155(data, i);
    }

    private String longChain155(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain156(data, i);
    }

    private String longChain156(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain157(data, i);
    }

    private String longChain157(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain158(data, i);
    }

    private String longChain158(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain159(data, i);
    }

    private String longChain159(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain160(data, i);
    }

    private String longChain160(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain161(data, i);
    }

    private String longChain161(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain162(data, i);
    }

    private String longChain162(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain163(data, i);
    }

    private String longChain163(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain164(data, i);
    }

    private String longChain164(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain165(data, i);
    }

    private String longChain165(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain166(data, i);
    }

    private String longChain166(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain167(data, i);
    }

    private String longChain167(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain168(data, i);
    }

    private String longChain168(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain169(data, i);
    }

    private String longChain169(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain170(data, i);
    }

    private String longChain170(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain171(data, i);
    }

    private String longChain171(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain172(data, i);
    }

    private String longChain172(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain173(data, i);
    }

    private String longChain173(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain174(data, i);
    }

    private String longChain174(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain175(data, i);
    }

    private String longChain175(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain176(data, i);
    }

    private String longChain176(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain177(data, i);
    }

    private String longChain177(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain178(data, i);
    }

    private String longChain178(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain179(data, i);
    }

    private String longChain179(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain180(data, i);
    }

    private String longChain180(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain181(data, i);
    }

    private String longChain181(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain182(data, i);
    }

    private String longChain182(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain183(data, i);
    }

    private String longChain183(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain184(data, i);
    }

    private String longChain184(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain185(data, i);
    }

    private String longChain185(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain186(data, i);
    }

    private String longChain186(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain187(data, i);
    }

    private String longChain187(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain188(data, i);
    }

    private String longChain188(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain189(data, i);
    }

    private String longChain189(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain190(data, i);
    }

    public String longChain190(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain191(data, i);
    }

    private String longChain191(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain192(data, i);
    }

    private String longChain192(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain193(data, i);
    }

    private String longChain193(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain194(data, i);
    }

    private String longChain194(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain195(data, i);
    }

    private String longChain195(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain196(data, i);
    }

    private String longChain196(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain197(data, i);
    }

    private String longChain197(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return longChain198(data, i);
    }

    private String longChain198(String data, int i) {
        String tmp = data;
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        data = tmp;
        return longChain199(data, i);
    }

    private String longChain199(String data, int i) {
        for (int j = 0; j < i; j++) {
            data = new StringBuilder().append(data).append(j).toString();
        }
        return data;
    }

    public static void main(String[] args) {
        // for(int i = 0; i < 200; i++){
        // String x = "private String longChain%s(String data,int i){\n"
        // + " data = new StringBuilder().append(data).append(i).toString();\n"
        // + " i++;\n"
        // + " return longChain%s(data,i);\n"
        // + " }\n";
        // String res = String.format(x,i,i+1);
        // System.out.println(res);
        // }
        // System.out.println(new LongChainUtil().longChain0("ls",0));
        System.out.println(new LongChainUtil().longChain150("ls", 10));
    }
}
